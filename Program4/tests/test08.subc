int bigger(int a, int b) {
  int res;
  res = a - b;
  if (res >= 0) {
    return 1;
  }
  return 0 - 1;
}

int main() {
  int result;
  int i;

  i = says("test08 : Expect = GTE : Actual = ");

  result = bigger(9, 4);
  if (result > 0) { i = says("GTE"); }
  if (result < 0) { i = says("LT"); }

  return 8;
}